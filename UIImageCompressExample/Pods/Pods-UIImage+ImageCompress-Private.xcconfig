#include "Pods-UIImage+ImageCompress.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/UIImage+ImageCompress" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/UIImage+ImageCompress"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}